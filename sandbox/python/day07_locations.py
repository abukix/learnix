"""
Day 7 — Locations & coordinates (sets & tuples)

Track visited: set[str] of location names.
    visit(visited, location) -> bool   # True if newly visited

Represent each location's coordinates as tuple[int, int] in:
    locations: dict[str, tuple[int, int]]

    distance(loc_a, loc_b) -> float   # basic distance formula
"""

import math  # noqa: F401


def visit(visited, location):
    # TODO: return True if this location wasn't already in the set
    pass


def distance(loc_a, loc_b):
    # TODO: loc_a and loc_b are (x, y) tuples — compute euclidean distance
    pass


if __name__ == "__main__":
    pass
