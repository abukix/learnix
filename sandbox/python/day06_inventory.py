"""
Day 6 — Inventory & quest log (dictionaries)

Add an inventory: dict[str, int] (item -> quantity) per character.
    add_item(inventory, item, qty)
    use_item(inventory, item, qty) -> bool   # fails gracefully if not enough

Add a quest_log: dict[str, str] (quest name -> status).
    update_quest(log, name, status)
"""


def add_item(inventory, item, qty):
    # TODO
    pass


def use_item(inventory, item, qty):
    # TODO: return False instead of crashing if there isn't enough
    pass


def update_quest(log, name, status):
    # TODO
    pass


if __name__ == "__main__":
    pass
