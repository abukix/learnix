"""
Day 11 — Hand-rolled test harness (no pytest)

Write your own test_cases-style list of dicts for at least:
    resolve_battle, buy_item, sort_by_level

Each case should carry its inputs and expected output. Then write
run_tests() that loops over the cases, calls the function under test,
and reports pass/fail per case — the same shape as boot.dev's own
main_test.py, built from scratch.
"""

test_cases = [
    # TODO: {"name": ..., "inputs": ..., "expected": ...} per case
]


def run_tests(cases):
    # TODO: loop over cases, call the function under test, compare to
    # expected, print PASS/FAIL per case with a summary at the end
    pass


if __name__ == "__main__":
    run_tests(test_cases)
